[package]
name = "github-actions-test"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/omelet1/github-actions-test"

[dependencies]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
